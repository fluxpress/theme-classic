<%- include('../_partials/navbar') %>

<main>
  <section>
    <h2>关于</h2>

    <img src="<%= user.avatar_url %>" alt="<%= user.name || user.login %>" />
    <p><%= user.name || user.login %></p>
  </section>
</main>
